package com.canhchim.repositories;

import com.canhchim.models.PrdImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrdImageRepository extends JpaRepository<PrdImage, Long>
{
}
